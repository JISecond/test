using UnityEngine;

public class DisplayCrickets : MonoBehaviour
{
    void Start()
    {
        // Выводим слово в лог при старте игры
        Debug.Log("crickets");
    }

    void Update()
    {
        // Выводим слово в лог каждый кадр (если нужно)
        Debug.Log("crickets");
    }
}
